Final review found a privileged bypass in the client candidate-create route. Fix backend only and run both npm run lint and npx tsx server/routes/recruitment.guards.test.ts.
- POST /requests/:requestId/candidates must require req.user.role === "client" exactly; admin/super_admin/manager/employee must receive 403 and use the admin sourcing route instead.
- Enforce for all callers that a candidate created on this route only belongs to a requestKind === "named_candidates" request and exactly to its owning client; do not rely on conditional checks that bypass for privileged roles.
- Apply the same exact-client role/ownership rule to client candidate document upload, requirements, detail/list and delete endpoints. Admin access must use separate admin routes, not client routes.
- Update the guard test: privileged calls to the client-submission route must be blocked, not allowed. Summarize results.
Show more
Let me read the current state of all relevant code first.

2 actions
Now read all six client-facing route handlers in full detail:

2 actions
Now I have a complete picture of all the problems. The issues are systematic across all 6 client routes: 1. requireClient middleware passes admin/super_admin/manager through — must become exact-role "client" only 2. Every handler has role === "client" ? {clientId: userId} : {_id only} SR query bypasses — admin gets unrestricted SR access through the client route 3. Every handler has if (…

Edited recruitment.routes.ts
Now rewrite all six client route handlers — replacing requireClient with requireExactClient and removing every role-conditional branch. Do them one at a time: **POST /requests/:id/candidates**

Edited recruitment.routes.ts
**GET /requests/:id/candidates**

Edited recruitment.routes.ts
**GET /requests/:id/candidates/:cid**

Edited recruitment.routes.ts
**DELETE /requests/:id/candidates/:cid**

Edited recruitment.routes.ts
**GET /requests/:id/candidates/:cid/documents/requirements**

Edited recruitment.routes.ts
**POST /requests/:id/candidates/:cid/documents** (upload)

Edited recruitment.routes.ts
**GET /requests/:id/candidates/:cid/documents/:did** (download)

Edited recruitment.routes.ts
Now update the file header comment block and the authorization matrix:

Edited recruitment.routes.ts
Now rewrite the guard test — remove the admin-bypass case for Guard 2, add explicit privilege-blocked assertions for every privileged role, and rename the guard function to remove the bypass:

2 actions
Both pass. Let me do one final scan to confirm there are no remaining requireClient usages on the client route section and no role-conditional SR queries left:

2 actions
1 failed
- requireClient is completely gone (not even defined anymore, the function was replaced entirely) - requireExactClient appears in the definition (line 102) and all 6 client route registrations (lines 234, 317, 359, 412, 474, 523, 657 — that's 7 counts for POST + GET list + GET detail + DELETE + GET requirements + POST docs + GET doc download) - The only role === "client" references are in th…
Working
Result
Done — both npm run lint and npx tsx server/routes/recruitment.guards.test.ts pass cleanly
Root cause of the bypass
The old requireClient middleware passed ["client", "admin", "super_admin", "manager"] through, and every handler then had role === "client" ? {clientId: userId} : {_id: req.params.id} SR queries and if (role !== "client") { return toAdminView(…) } short-circuits. Any privileged principal could call a client route and receive full internal data with no ownership check.

Changes — server/routes/recruitment.routes.ts
New requireExactClient middleware (replaces the old requireClient):

role !== "client"  →  403 "هذا المسار مخصص للعملاء فقط — يرجى استخدام مسارات الإدارة"

admin, super_admin, manager, employee are all rejected at the gateway. They must use /admin/… routes.

All 6 client route handlers rewritten unconditionally:

Route	Change
POST /requests/:id/candidates	requireExactClient; SR query always {clientId: userId}; kind check is now unconditional (no if role === "client" wrapper)
GET /requests/:id/candidates	requireExactClient; SR query always {clientId: userId}; if (role !== "client") { toAdminView } branch deleted
GET /requests/:id/candidates/:cid	requireExactClient; SR query always {clientId: userId}; if (role !== "client") { toAdminView } branch deleted
DELETE /requests/:id/candidates/:cid	requireExactClient; SR query always {clientId: userId}; if (role === "client") wrapper around ownership checks deleted — checks are now unconditional
GET …/documents/requirements	requireExactClient; SR query always {clientId: userId}; if (role === "client") wrapper deleted
POST …/documents (upload)	requireExactClient; SR query always {clientId: userId}; all if (role === "client") wrappers deleted — isAdminSourced, submittedBy, and required-category checks are now always applied
GET …/documents/:did (download)	requireExactClient; SR query always {clientId: userId}; if (role === "client") wrapper deleted
Changes — server/routes/recruitment.guards.test.ts
Guard 0 added — requireExactClient decision function. Tests all 5 roles:

"client" → allowed
"admin", "super_admin", "manager", "employee" → all blocked
Guard 2 updated — canClientSubmitCandidate no longer accepts a role parameter (the bypass if (role !== "client") return true is deleted). It tests only the requestKind. Added assertions proving privileged roles are blocked at Guard 0 before Guard 2 is ever reached.

Guards 5 & 6 updated — clientDocUploadBlocked and clientDeleteBlocked no longer accept a role parameter (requireExactClient already enforces it).

New completeness assertions:

allowedOnClientRoutes  === ["client"]                              ✓
blockedFromClientRoutes === ["admin","super_admin","manager","employee"]  ✓

Total assertions: 32 (up from 16), all passing.


